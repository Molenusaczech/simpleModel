let camera, scene, renderer;

function initGlobals(_camera, _scene, _renderer) {
    camera = _camera;
    scene = _scene;
    renderer = _renderer;
}

export { camera, scene, renderer, initGlobals };